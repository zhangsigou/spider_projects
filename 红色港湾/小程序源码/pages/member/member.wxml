<!--pages/member/member.wxml-->
<!-- 内容 -->
	<view class="cu-form-group" wx:for="{{5}}" wx:key="key" bindtap="nav">
	 <view class="paddinglr">
    <view class="solidbottom" wx:for="{{list}}" wx:key="key">
    <view class="wz">
    <view>{{item.wz}}</view>
     <view>{{item.wzb}}</view>
     </view>
     <view class="rq">
     <text >{{item.wzc}}</text></view>
    </view>
  </view>
		<view class="cuavatar " wx:for="{{images}}" wx:key="key"><image src="{{item.tp}}"></image></view>
	</view>

